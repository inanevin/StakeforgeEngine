<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="style_button_default" TargetType="{x:Type Button}">
        <Setter Property="Height" Value="{StaticResource height_item_base}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Background" Value="{StaticResource brush_background2}"/>
        <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource brush_gray}"/>
        <Setter Property="FontSize" Value="{StaticResource size_default_font}"/>
        <Setter Property="FontFamily" Value="{StaticResource font_default}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            CornerRadius="1"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource brush_background_light0}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource brush_background1}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="style_label_icon" TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="{StaticResource font_icons}"/>
    </Style>

    <Style x:Key="style_label_default" TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
        <Setter Property="FontSize" Value="{StaticResource size_default_font}"/>
        <Setter Property="FontFamily" Value="{StaticResource font_default}"/>
    </Style>

    <!-- Window Buttons -->
    <Style x:Key="style_button_window" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource brush_background0}"/>
        <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
        <Setter Property="FontFamily" Value="{StaticResource font_icons}"/>
        <Setter Property="FontSize" Value="{StaticResource size_icon_font}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Width" Value="{StaticResource height_item_big}"/>
        <Setter Property="Height" Value="{StaticResource height_item_base}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
               BorderBrush="{TemplateBinding BorderBrush}"
               CornerRadius="0"
               BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource brush_accent_primary}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource brush_background1}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="style_button_window_close" TargetType="{x:Type Button}" BasedOn="{StaticResource style_button_window}">

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
                     BorderBrush="{TemplateBinding BorderBrush}"
                     CornerRadius="0"
                     BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource brush_accent_purple_light}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource brush_background1}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        
    </Style>

    <Style x:Key="style_button_icon" TargetType="{x:Type Button}">
    <Setter Property="BorderThickness" Value="0"/>
     <Setter Property="FontSize" Value="{StaticResource size_icon_font}"/>
    <Setter Property="Width" Value="{StaticResource height_item_base}"/>
    <Setter Property="Height" Value="{StaticResource height_item_base}"/>
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="FontFamily" Value="{StaticResource font_icons}"/>
    <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
    <Setter Property="BorderBrush" Value="{StaticResource brush_gray}"/>


    <Setter Property="Template">
        <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
                <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  CornerRadius="1"
                  BorderThickness="{TemplateBinding BorderThickness}">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="BorderThickness" Value="1"/>
                    </Trigger>
                    <Trigger Property="IsPressed" Value="True">
                        <Setter Property="Background" Value="{StaticResource brush_background1}"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </Setter.Value>
    </Setter>
    </Style>

    <Style x:Key="style_button_icon_small" TargetType="{x:Type Button}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontSize" Value="{StaticResource size_icon_font_small}"/>
        <Setter Property="Width" Value="{StaticResource height_item_base_small}"/>
        <Setter Property="Height" Value="{StaticResource height_item_base_small}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="FontFamily" Value="{StaticResource font_icons}"/>
        <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource brush_gray}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="{TemplateBinding Background}"
              BorderBrush="{TemplateBinding BorderBrush}"
              CornerRadius="1"
              BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderThickness" Value="1"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{StaticResource brush_background1}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>