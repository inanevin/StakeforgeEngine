<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:main="clr-namespace:StakeforgeEditor.Main"
                    xmlns:widgets="clr-namespace:StakeforgeEditor.Widgets"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <DropShadowEffect x:Key="fx_drop_shadow_default" BlurRadius="4" Opacity="0.8" ShadowDepth="1"/>

    <Style x:Key="style_context_menu_item" TargetType="{x:Type widgets:MenuItemDefault}" BasedOn="{StaticResource {x:Type MenuItem}}">
        <Setter Property="Background" Value="{StaticResource brush_background0}"/>
        <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
        <Setter Property="FontFamily" Value="{StaticResource font_default}"/>
        <Setter Property="FontSize" Value="{StaticResource size_default_font}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type widgets:MenuItemDefault}">
                    <Grid>
                        <Border Name="Border"
                            Height="{StaticResource height_item_base}"
                            Background="{TemplateBinding Background}" MinWidth="100" 
                            >
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Label Grid.Column="0" Width="{StaticResource height_item_base}" Foreground="{TemplateBinding HeaderIconColor}" Content="{TemplateBinding HeaderIcon}" Style="{StaticResource style_label_icon_small}"/>

                                <ContentPresenter Grid.Column="1"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>

                                <Path x:Name="RightArrow"
                                  Grid.Column="1"
                                  Margin="5,0,5,0"
                                  Visibility="Collapsed"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  Data="M0,0 L4,3.5 L0,7 z"
                                  Fill="{TemplateBinding Foreground}"/>
                            </Grid>
                        </Border>

                        <Popup Name="PART_Popup"
                           AllowsTransparency="True"
                           Placement="Right"
                           IsOpen="{TemplateBinding IsSubmenuOpen}"
                           Focusable="False">
                            <Border BorderThickness="1"
                                BorderBrush="{StaticResource brush_background_light0}"
                                Background="{StaticResource brush_background0}"
                                Effect="{StaticResource fx_drop_shadow_default}">
                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="True">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource brush_accent_purple}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="style_context_menu" TargetType="{x:Type ContextMenu}">
        <Setter Property="DataContext" Value="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border>
                        <Border.Style>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="Tag" Value="{DynamicResource {x:Static SystemParameters.DropShadowKey}}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource brush_background0}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource brush_background_light0}"/>
                                        <Setter Property="BorderThickness" Value="{StaticResource context_border}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel ClipToBounds="True" Orientation="Vertical" IsItemsHost="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Resources>
            <Style TargetType="{x:Type widgets:MenuItemDefault}" BasedOn="{StaticResource style_context_menu_item}"/>
        </Style.Resources>
    </Style>


</ResourceDictionary>