<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:main="clr-namespace:StakeforgeEditor.Main"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <DropShadowEffect x:Key="MyDropShadowEffect" BlurRadius="4" Opacity="0.8" ShadowDepth="1"/>

    <Style x:Key="style_context_menu_item" TargetType="{x:Type MenuItem}">
        <Setter Property="Background" Value="{StaticResource brush_background0}"/>
        <Setter Property="Foreground" Value="{StaticResource brush_text_primary}"/>
        <Setter Property="FontFamily" Value="{StaticResource font_default}"/>
        <Setter Property="FontSize" Value="{StaticResource size_default_font}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type MenuItem}">
                    <Grid>
                        <Border Name="Border"
                            Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource brush_background_light0}"
                            BorderThickness="0, 0, 0, 1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ContentPresenter Grid.Column="0"
                                              Margin="10,3"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              VerticalAlignment="Center"/>

                                <Path x:Name="RightArrow"
                                  Grid.Column="1"
                                  Margin="5,0,5,0"
                                  Visibility="Collapsed"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Center"
                                  Data="M0,0 L4,3.5 L0,7 z"
                                  Fill="{TemplateBinding Foreground}"/>
                            </Grid>
                        </Border>

                        <Popup Name="PART_Popup"
                           AllowsTransparency="True"
                           Placement="Right"
                           IsOpen="{TemplateBinding IsSubmenuOpen}"
                           Focusable="False">
                            <Border BorderThickness="1"
                                BorderBrush="{StaticResource brush_background_light0}"
                                Background="{StaticResource brush_background0}"
                                Effect="{StaticResource MyDropShadowEffect}">
                                <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                            </Border>
                        </Popup>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="True">
                            <Setter TargetName="RightArrow" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{StaticResource brush_accent_purple}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="style_context_menu" TargetType="{x:Type ContextMenu}">
        <Setter Property="DataContext" Value="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ContextMenu}">
                    <Border>
                        <Border.Style>
                            <Style TargetType="{x:Type Border}">
                                <Setter Property="Tag" Value="{DynamicResource {x:Static SystemParameters.DropShadowKey}}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="True">
                                        <Setter Property="Background" Value="{StaticResource brush_background0}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource brush_background_light0}"/>
                                        <Setter Property="BorderThickness" Value="{StaticResource context_border}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel ClipToBounds="True" Orientation="Vertical" IsItemsHost="True" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Resources>
            <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource style_context_menu_item}"/>
        </Style.Resources>
    </Style>

    <ContextMenu x:Key="context_menu_dock_tab" Style="{StaticResource style_context_menu}">
        <MenuItem Header="World" Command="{Binding DataContext.AddPanelCommand}" CommandParameter="World"/>
        <MenuItem Header="Entities" Command="{Binding DataContext.AddPanelCommand, RelativeSource={RelativeSource AncestorType={x:Type main:DockArea}}}" CommandParameter="Entities"/>
        <MenuItem Header="Resources" Command="{Binding DataContext.AddPanelCommand, RelativeSource={RelativeSource AncestorType={x:Type main:DockArea}}}" CommandParameter="Resources"/>
        <MenuItem Header="Console" Command="{Binding DataContext.AddPanelCommand, RelativeSource={RelativeSource AncestorType={x:Type main:DockArea}}}" CommandParameter="Console"/>
        <MenuItem Header="Properties" Command="{Binding DataContext.AddPanelCommand, RelativeSource={RelativeSource AncestorType={x:Type main:DockArea}}}" CommandParameter="Properties"/>
    </ContextMenu>

</ResourceDictionary>